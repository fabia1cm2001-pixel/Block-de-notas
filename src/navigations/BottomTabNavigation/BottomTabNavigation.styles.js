import { StyleSheet } from "react-native";

export const styles = new StyleSheet.create({
  tabBarStyle: {
    backgroundColor: "#1e293b",
    borderTopWidth: 0,
    height: 60,
    paddingBottom: 8,
    paddingTop: 8,
  },
});